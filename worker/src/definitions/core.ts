export type TargetStore = {
  storeName: string;
  storeLink: string;
};

export type CollectedStoreInfo = {
  storeName: string;
  storeLink: string;
  email?: string;
  searchTerm?: string;
  storeOwner?: string;
  businessRegNum?: string;
  ecomRegNum?: string;
  address?: string;
};
